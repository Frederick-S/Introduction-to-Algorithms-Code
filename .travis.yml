language: python
python:
  - 2.7
  - 3.3
  - 3.4
  - 3.5
install:
  - pip install coveralls
  - pip install pep8
script:
  - pep8 .
  - coverage run --source=src setup.py test
after_success:
  coveralls
